# --- secrets / local config ---
.env.local
cookies.txt
*.log

# --- account state (contains real login sessions + emails) ---
accounts/

# --- databases (API keys, task history, account metadata) ---
*.db
*.db-journal
*.db-wal
*.db-shm

# --- generated media ---
downloads/

# --- probe / debug artifacts (contain live session snippets) ---
/*.html
/*.png
/*.jpg
/*.json
/*.txt
!requirements.txt
!README.md

# --- one-off debug script (contains a signed CDN url) ---
dl_now.py

# --- python ---
__pycache__/
.venv/
*.pyc

# --- third-party binaries analysed locally (150MB) ---
*.exe
accounts_fb/
logs/

desktop/renderer/node_modules/
desktop/node_modules/

# Ban dong goi + runtime tai ve khi build
dist-app/
build/

# Rác máy local / bản sao cấu hình / file của bên thứ ba
.DS_Store
*.bak
DomixHub-Seedance.exe

# Canvas Claude Design đã đóng gói (2MB, sinh lại từ docs/design/*.dc.html)
docs/design/*.html
!docs/design/*.dc.html
broker_secret
.claude/
*.mp4
tasks.lock
